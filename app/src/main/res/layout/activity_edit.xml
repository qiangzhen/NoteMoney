<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="#000">

        <ImageView
            android:id="@+id/img_edit_bcak"
            android:layout_width="50dp"
            android:layout_height="match_parent"
            android:background="@drawable/ic_menu_back"
            android:scaleType="centerCrop" />

        <ImageView
            android:id="@+id/img_edit_submit"
            android:layout_width="50dp"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:background="@drawable/ic_menu_mark"
            android:scaleType="centerCrop" />
    </RelativeLayout>


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center">

        <EditText
            android:id="@+id/et_edit_out_money"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@android:drawable/edit_text"
            android:gravity="left"
            android:hint="金额"
            android:textColor="#000" />

        <Spinner
            android:id="@+id/sp_edit_out_category"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_below="@id/et_edit_out_money"
            android:background="@android:drawable/edit_text"
            android:hint="饮食"
            android:textColor="#000"></Spinner>

        <Spinner
            android:id="@+id/sp_edit_out_sub_category"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_below="@id/sp_edit_out_category"
            android:background="@android:drawable/edit_text"
            android:hint="水果"
            android:textColor="#000"></Spinner>

        <Button
            android:id="@+id/btn_edit_submit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignRight="@id/sp_edit_out_sub_category"
            android:layout_below="@id/sp_edit_out_sub_category"
            android:layout_gravity="center_horizontal|bottom"
            android:background="@drawable/shape_btn"
            android:text="确认修改"
            android:textSize="28sp" />
    </RelativeLayout>

</LinearLayout>